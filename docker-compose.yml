version: "3.9"

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: trading-bot
    env_file:
      - .env
    volumes:
      - ~/tradingbot-logs:/app/logs
    restart: unless-stopped


